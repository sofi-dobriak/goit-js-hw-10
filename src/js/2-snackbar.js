const refs = {
  delayInput: document.querySelector('input[name="delay"]'),
  submitBth: document.querySelector('button'),
};

// ================== STYLES ==================
refs.delayInput.classList.add('delay-input');
refs.submitBth.classList.add('delay-button');
// =============================================
